<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp1.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MauiApp1"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiApp1">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->
    <TabBar>
        <Tab Title="page 1">
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}"/>
        </Tab>
        <Tab Title="page 2">
            <ShellContent ContentTemplate="{DataTemplate views:MainPagee}"/>
        </Tab>
        <Tab Title="page 3">
            <ShellContent ContentTemplate="{DataTemplate views:MainPageee}"/>
        </Tab>
        <Tab Title="page 4">
            <ShellContent ContentTemplate="{DataTemplate views:MainPageeee}"/>
        </Tab>
    </TabBar>

</Shell>
